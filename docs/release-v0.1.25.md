# 回合回溯 v0.1.25

## 修复

- “每场战斗仅触发一次”的 Glam 附魔状态现在严格跟随所选回合快照。
- 回到附魔实际触发前：恢复为可用状态，允许在新时间线中再次触发。
- 回到附魔实际触发后：保留已使用和禁用状态，不会错误重复触发。
- 删除 v0.1.21 引入的跨快照单调消耗集合；快照中的 `EnchantmentStatus` 与 `UsedThisCombat` 重新成为唯一恢复依据。

## 验证

- Android v0.110.1 实机进入双史莱姆战斗。
- 给爪击添加 Glam 附魔并真实打出，确认附魔变为已使用。
- 回溯到打出前，确认附魔恢复可用。
- 再回溯到打出后，确认附魔保持已消耗。
- 测试日志结果：`PASS`。
- PC v0.107.1 生产模组与专项测试模组均通过编译；当前测试机 Steam 未登录，因此没有声称完成 PC 实机运行测试。

## 安装包

- `TurnRewind-v0.1.25-Mobile-v110.1.zip`
- `TurnRewind-v0.1.25-PC-v107.1.zip`

本次按要求不生成 Android v103 或 v111 安装包。
